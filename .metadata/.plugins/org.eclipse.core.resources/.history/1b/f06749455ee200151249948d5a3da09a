var format = {
	color : "fff",
	top : 20,
	//backgroundColor : "fff",
	font: {fontSize: 60}
};

var buildUI = function(wObj) {
	var win = Ti.UI.createWindow({
		backgroundImage : "background.png",
		backgroundRepeat: "repeat-y",
		layout : "vertical"
	});
	// var latLbl = Ti.UI.createLabel(format);
	// latLbl.text = "Latitude: " + lat;
	// var lngLbl = Ti.UI.createLabel(format);
	// lngLbl.text = "Longitude: " + lng;
	// var icon = Ti.UI.createImageView({
		// image: wObj.icon,
		// top: 20
	// });
	var iconLbl = Ti.UI.createLabel(format);
	iconLbl.text = "Conditions: " + wObj.conditions;
	var timeLbl = Ti.UI.createLabel(format);
	timeLbl.text = wObj.time;
	var whereLbl = Ti.UI.createLabel(format);
	whereLbl.text = wObj.city + ", " +wObj.country;
	var tempLbl = Ti.UI.createLabel(format);
	tempLbl.text = "High: " + wObj.high + ", Low: " + wObj.low;
	var dateLbl = Ti.UI.createLabel(format);
	dateLbl.text = "Date: " + wObj.month + "/" + wObj.day + "/" + wObj.year;
	
	//win.add(latLbl);
	//win.add(lngLbl);
	// win.add(icon);
	win.add(iconLbl);
	win.add(dateLbl);
	win.add(timeLbl);
	win.add(whereLbl);
	win.add(tempLbl);
	
	
	win.open();
};

exports.buildUI = buildUI;
